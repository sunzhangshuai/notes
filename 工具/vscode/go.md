## 快捷片段语言

1. Command + shuit + p

2. 输入：snippets

3. 选择：go

4. ```go
   编写：
   "pln": {
   		"prefix": "pln",
   		"body": [
   			"fmt.PrintIn($0)"
   		],
   		"description": "fmt.PrintIn()"
   }
   ```

5. 效果：pln -> fmt.PrintIn($0)